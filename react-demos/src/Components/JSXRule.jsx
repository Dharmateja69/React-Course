import React from "react";

const JSXRule = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>jsx must return a single parent element</p>
      <p>jsx elements must be properly closed</p>
      <p>
        jsx attributes are written using camelCase(className instead of class
        name )
      </p>
    </div>
  );
};

export default JSXRule;
